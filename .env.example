# Copy to `.env` and fill in real values. Never commit `.env`.

# --- Dashboard backend (read-only, multi-project) ---
# Path glob to metaswarm-managed project roots
METASWARM_PROJECTS_GLOB=

# --- Optional: GitHub for read-only PR/CI status ---
GITHUB_TOKEN=
